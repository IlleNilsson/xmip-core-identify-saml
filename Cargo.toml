# Its own workspace root, so this repository builds on its own.
[workspace]

[package]
name = "xmip-core-identify-saml"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
repository = "https://github.com/IlleNilsson/xmip-core-identify-saml"
description = "Identify by saml: reads a SAML 2.0 assertion's subject and issuer, unverified, from a posted response or from the content, with the assertion riding as proof for the second gate. A xmip-core-identify technology."
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
# A Technology depends on its parent Capability, never the reverse
# (repository-model.md). The mechanism is xmip-core's; the Message is the
# Foundation's, for the message layer this mechanism also travels on.
identify = { package = "xmip-core-identify", git = "https://github.com/IlleNilsson/xmip-core-identify", branch = "main" }
xcore = { package = "xmip-core", git = "https://github.com/IlleNilsson/xmip-core", branch = "main" }
message = { package = "xmip-core-message", git = "https://github.com/IlleNilsson/xmip-core-message", branch = "main" }
# XML character data, unescaped as every reader in the estate does.
codec = { package = "xmip-core-codec", git = "https://github.com/IlleNilsson/xmip-core-codec", branch = "main" }
# The POST binding carries the response as standard base64 (SAML Bindings
# 3.5.4); the estate's base64 crate.
base64 = "0.22"

[dev-dependencies]
# The context and Stream a Message is built over, for the tests only.
context = { package = "xmip-core-context", git = "https://github.com/IlleNilsson/xmip-core-context", branch = "main" }
stream = { package = "xmip-core-stream", git = "https://github.com/IlleNilsson/xmip-core-stream", branch = "main" }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
too_many_lines = "warn"
pedantic = { level = "warn", priority = -1 }
missing_errors_doc = "warn"
missing_panics_doc = "warn"
unwrap_used = "warn"
expect_used = "allow"
